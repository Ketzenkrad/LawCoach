<?php get_header($name); ?>
    <?php if ( have_posts() ) : while ( have_posts() ) : the_post(); ?>
    <?php endwhile; ?>
    <!-- Навигация -->
    <?php else: ?>
    <!-- Посты не найдены -->
    <?php endif; ?>
<?php get_footer($name); ?>